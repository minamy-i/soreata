# NEXT

更新：2026-06-28
push：後日改めて

## 次回の冒頭でやること
今日やったことの解説（下記）を読んでから、Phase 4に進む。

## 今日やったこと（2026-06-28）の解説

### 設計フェーズ
- v1の実装範囲を決めた（当人の動線のみ）
- 保存フロー・UI出し分け・ダッシュボード仕様を決定
- SPEC・NEXT・docs/ を整理してコミット

### 実装フェーズ

**Phase 1：Next.js プロジェクト作成**
今まで `index.html` 1枚だったアプリを、本格的なWebアプリの骨格に変えた。
Next.js はページ・API・認証などをまとめて扱えるフレームワーク。

**Phase 2：Supabase クライアント設定**
アプリからデータベースに接続するための「接続口」を作った。
`lib/supabase.ts` がそれ。
接続先のURL・APIキーは `.env.local` に書いてある（Gitには上がらない）。

**Phase 3：認証基盤**
Googleログインの仕組みを組み込む準備をした。
- Supabaseダッシュボードで「Googleでログイン」を有効化
- Google Cloud Consoleで「このアプリはGoogleログインを使います」という登録をした（OAuth）
- コード側にログイン後の処理（コールバック）を追加：`app/auth/callback/route.ts`

まだ画面は何も変わっていない。
次のPhase 4（テーブル作成）からデータの保存・取得ができるようになり、
Phase 5以降で実際の画面を作っていく。

## RPD 残量
2026-06-18:55/500, 19:44/500, 20:62/500, 21~:44/500

## 次にやること

### Next.js移行・v1実装

技術方針：Next.js（App Router・TypeScript）+ Supabase

#### Phase 1：プロジェクト設定
- Next.js プロジェクト新規作成（TypeScript + App Router）
- Supabase プロジェクト作成（外部作業・開発者が行う）
- 環境変数設定（GEMINI_API_KEY・Supabase接続情報）

#### Phase 2：AI分析機能の移植
- `index.html` → Next.js の `/` ページ
- `api/decompose.js` → Next.js API route
- `prompt.js` → TypeScript モジュール

#### Phase 3：認証
- Supabase Auth（Google OAuth）設定
- ログインボタン・UI出し分け
- 初回ログイン後のリダイレクト

#### Phase 4：DBテーブル作成
- accounts・persons・records テーブル
- RLS（行レベルのアクセス制御）設定

#### Phase 5：当人ダッシュボード
- アカウント設定画面
- AI分析記録一覧

#### Phase 6：保存機能
- 「保存する」ボタン・abilities JSON保存
- 記録詳細へのリダイレクト

#### Phase 7：記録詳細
- 能力確認日入力・削除

---

### 記録機能の追加

v1はSPECに記載済み。以下はv2以降の設計。

#### ユーザー種別（v2）
ユーザーは2種類。ログインの入り口は1つ（Googleでログイン）で共通。
システムがDB判定で振り分ける。

| | 当人アカウント | 協力者アカウント |
|---|---|---|
| 誰が作る | 初回ログイン時（招待なし） | 当人からの招待リンク経由（初回のみ） |
| ログイン後 | 当人ダッシュボードへ | 協力者ダッシュボードへ |
| 見られるデータ | 自分の記録のみ | 担当当人の記録（権限の範囲内） |

#### 認証・登録フロー（v2・協力者対応）
- 初回・招待リンクなし → 当人新規登録（ニックネーム入力）→ 当人ダッシュボード
- 初回・招待リンク経由 → 協力者新規登録（ニックネーム入力）→ 協力者ダッシュボード
- 2回目以降 → DBのアカウント種別で自動振り分け
- メアドは表示のみ（Googleアカウントで管理・アプリ側での変更なし）
- 当人がGoogleアカウントを失った場合：管理者が手動で別アカウントに移行（owner履歴テーブルで記録）

#### 招待フロー（協力者登録）
1. 当人がアプリ内で協力者のメアドを入力
2. サーバーがトークンを生成し、招待テーブルに保存
3. トークン入りのURLをメールで協力者に送信（外部メールサービスが必要）
4. 協力者がリンクをクリック → Googleログイン
5. サーバーがトークンを確認 → 協力者テーブルに紐付けを保存
6. 協力者がニックネームを登録 → 協力者ダッシュボードへ

#### テーブル構成
`docs/data_structure.md` 参照

#### アクセス権（当人ダッシュボード・v2）
当人ダッシュボードは当人・協力者が同一URL（/home/[id]）でアクセスする。
ログイン中のアカウント種別で表示を切り替える。

| 機能 | 当人 | 協力者 |
|---|---|---|
| アカウント設定 | 編集可 | 閲覧不可 |
| 協力者一覧 | 管理可 | 閲覧可 |
| AI分析記録 | 作成・閲覧可（編集不可） | 作成・閲覧可（編集不可） |
| └ 能力確認日 | 新規入力・変更可 | 新規入力可・変更不可 |
| 観察記録 | 作成・自分のもの編集可・閲覧可 | 作成・自分のもの編集可・閲覧可 |
| 会議室 | 投稿・コメント・まとめ依頼・承諾可 | 投稿・コメント・まとめ記述可 |

#### 会議室（v2）
提案・相談・連絡の場。観察記録に書く前の事前相談・合意形成に使う。

- 全員（当人・協力者）が投稿・コメントできる
- 当人が「まとめ依頼」ボタンを押すと投稿・コメントがロックされる
- 誰かがまとめを書く（summary_text）
- 当人が「観察記録へ書き込み」ボタンを押したら完了（自動書き込み）
- 却下した場合はロック解除・summary_textは残す・議論に戻る

#### 画面構成（v2以降）
画面構成図：`docs/screen_tree.md` 参照

**協力者ダッシュボード（URL未定）**
- 担当当人の一覧
- アカウント設定（ニックネーム編集・メアド表示のみ）
- 当人を選択 → 当人ダッシュボード（/home/[id]）へ

**観察記録（/home/[id]/memo）**
- 追記型タイムライン
- 全員（当人・協力者）が投稿可
- 作成者のみ編集可
- タイトル必須

**会議室（/home/[id]/meeting・仮）**
- 提案・相談・連絡の投稿・コメント（全員）
- 当人がまとめ依頼→誰かがまとめ記述→当人が承諾→観察記録へ自動書き込み

#### URL構成（v2以降）
- `/home/[id]/memo`：観察記録タイムライン
- `/home/[id]/meeting`：会議室（仮）
- `/home/[id]/collaborators`：協力者一覧
- 協力者ダッシュボード：URL未定

#### 未決
- 協力者ダッシュボードのURL
- 会議室のURL（/home/[id]/meeting は仮）

---

## 能力補完の観点を持つ専門家への相談（実現できたら）

### soreataの説明
- 「できて当たり前」とされる行動・指示を、要素と必要な能力に分解し対策を提案するWebアプリ
- 診断でなく課題の解剖図。人を評価しない、行動を変えようとしない
- 課題の中に折り畳まれた能力を掘り出すことが目的
- AIが6カテゴリ（人・物・動作・空間・時間・知識）で内部分析し、能力ごとに対応を提案する
- 「掘れる人の目」を掘れない人に貸す道具

### 相談したいこと
- 見落としやすい能力パターンを教えてもらう
- 6カテゴリが妥当かどうか確認する
- EXAMPLES.mdの例を読んでもらい、能力分析がずれていないか確認する
- よくある困りごとと背景にある能力を教えてもらう（few-shot例の素材として）
- 対応策の方向性が的外れでないか確認する
