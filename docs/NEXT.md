# NEXT

更新：2026-07-18
push：後日改めて（app/home/[team_id]配下3ファイル・login/page.tsx・page.tsx・lib/require-member.ts・docs/SPEC.mdが未commit）

## 現在の方針
soreata は段階（旧「v1」「v2」）で分けず、一つの完成形を最初から設計・構築する。
DB・骨格（アカウント・チーム・権限・招待の中核構造）は完成形で決める。
実装だけは依存順に少しずつ進める。
決定経緯 → `docs/NOTES.md`「v1/v2 の段階分けを廃止・一つの完成形として設計する」参照

docs/NOTES.mdは骨子（DB構造・権限・認証・招待などの中核設計）に関わる決定のみを書く。
UI文言・配色・アイコン等の見た目の微調整は、逐一書くと堂々巡りになるため対象外とする（2026-07-14決定）。

## 次にやること

### SPEC.md 文書整理（進行中、2026-07-18）
GitHub提出に向けた文書整理。docs/SPEC.md内の重複記述をA〜Gの7項目で洗い出し、順に整理中。行番号は2026-07-18時点。

- [x] A：対象者確定／対象者未確定（旧チーム確定／お試し）の定義重複。語彙定義に集約し、関連箇所（入口による出し分け・保存フロー・結果エリア）を修正済み。副次的に「保存先候補」という用語も廃止、「キャンセル」→「取消」の表記統一漏れも修正
- [x] B：未ログイン時のリダイレクト先。`/account`と`/home/[team_id]`配下で挙動が食い違っていたため、「未ログインはどのURLでも`/`（AI提案画面）へ誘導、例外は`/`自身と`/login`」に統一。`lib/require-member.ts`のrequireSession/requireMember、呼び出し元3ファイル、app/login/page.tsx、app/page.tsxのコード修正込み（next引数を廃止）
- [x] C：「AI提案へ」リンク＝保存の入口、の説明重複（L121チームページ／L180保存フロー冒頭）。URL遷移先の一次情報はL121に残し、L180は「チームページ参照」に置換して対象者確定ロジックのみ記述
- [x] D：保存確認ダイアログの文言「チーム○○に保存しますか？」の重複（L80結果エリア／L186保存フロー）。結果エリアはボタン配置・表示条件のみ、押下後の挙動（確認パネルの文言・ボタン構成・排他表示）は保存フロー側に一本化
- [x] E：「コピー機能と同じテキスト」（外部ツール投稿の説明）の重複（L174記録詳細／L244検討中の仕様）。外部ツール投稿は機能仕様側（チーム設定・記録詳細）で既に確定記述されていたため、検討中の仕様の「外部ツールへの投稿（Slack/Discord連携）」節を削除し、スキーマ項目・制約はチーム設定（現L158〜161）に統合。「コピー機能と同じテキスト」は記録詳細（現L175）のみに残す
- [ ] F：Webhook管理を委譲された協力者の見える範囲の説明が分散（チームページの表／チーム設定。旧・検討中の仕様分はEの整理でチーム設定に統合済みのため実質2箇所に減）
- [x] G：「白に近い明るさの文字＋ピンアイコン」現在地表示の説明が個別記述されている箇所の整理（グローバルナビ／チームページ・チーム設定・記録詳細のページタイトル）。チーム名タグは現在地でなく対象チーム名の提示のため対象から除外し、ピンアイコンを外した（2026-07-18、app/page.tsx・app/globals.css反映済み）。加えて画面仕様の先頭に「統一デザイン（全画面共通）」を新設し、現在地表示・チーム名タグとの違い・アクション行の配置をここに一本化。各画面側は「統一デザイン参照」のみに置換

別件「AI分解」→「AI提案」の呼称統一（画面名・記録名）：完了（2026-07-18）。SPEC.md全箇所とコード内コメント（page.tsx・record/[recordId]/page.tsx・AbilityBody.tsx・use-session.ts・use-active-team.tsx・team-empty.ts・record-text.ts・ability.ts）を統一済み。「AI提案へ」リンク・「AI提案の記録」との衝突は無かった

### SPEC.md 全体構成の見直し（進行中、2026-07-18）
A〜G整理の過程で、旧「機能仕様」を画面仕様（何が表示されているか）と機能仕様（画面をまたぐ処理・データ操作）に分割し、以下を新設した。行番号を使った参照はこの再構成で無効になっているため、以後は見出し名で参照する。
- 画面仕様：グローバルナビ・フッター・AI提案画面・ログインページ・マイページ・チームページ・チーム設定・記録詳細の各画面を1画面1項目に整理
- 画面仕様の先頭に「統一デザイン（全画面共通）」を新設（現在地表示・アクション行の配置など、複数画面にまたがる見た目のルールを集約）
- 機能仕様に「画面遷移」を新設（ログイン後の遷移先・ブランドロゴ経由の制約など、注意が必要な遷移だけを集約。単純な画面構成は`docs/screen_tree.md`参照に一本化）
- `docs/screen_tree.md`を最新化（AI分解→AI提案の表記統一、チーム作成・脱退・記録削除など抜けていた機能を追加、A_DASHの権限区分を修正）

未対応：機能仕様側（アクセス制御・認証登録フロー・チーム作成フロー・保存フロー・招待フロー）はまだ内容の精査前。F完了後に着手するか要相談。

### アカウント中心モデルへの再設計（進行中）

決定経緯 → `docs/NOTES.md`「DBテーブル刷新：アカウント中心モデルへの再設計」「アカウント入口・招待フローの確定」参照

以下2項目（owner移譲・中身があるチームの削除）は、ここまでの実装分が固まってから着手する（2026-07-07）。

- [ ] 当人権限（owner）移譲（指名・/account受諾・受諾RPC）：後回し（2026-07-06、着手前確認で保留）
  - 着手前に要確認・未回答：
    - 移譲が承諾されると、旧当人権限者はそのチームからどうなるか（NOTES.mdの記述では「旧owner行もrevoked_at」＝チームから完全に外れる、となっているが、実際の挙動として影響が大きいため再確認が必要）
    - 当人権限の移譲先の指名は、1チームにつき同時に1件のみ（既存の保留を取り消してから次を指名）にするか、複数人へ同時に指名できるようにするか
  - アプリ全体（招待・投稿まで）が動いてから着手
  - UI詳細（指名の場所・表示）は着手前に相談
- [ ] 中身があるチームの削除
  - 記録・協力者を全て消して空にしてから削除する方式（却下はしない。docs/SPEC.md「中身があるチームの削除」参照）
  - 協力者の除名手段（現状は招待の取消のみ）の設計が必要。最も重いため最後
